<div class="modal fade" id="quoteModal" tabindex="-1" role="dialog" aria-labelledby="quoteModalLabel" aria-hidden="true" style="z-index: 2000;">
  <div class="modal-dialog modal-dialog-centered modal-lg rounded-0" role="document">
    <div class="modal-content rounded-0 mb-med">
      
      <div class="modal-body py-mini">
        
        <button type="button" class="close" data-dismiss="modal" aria-label="Close" style="position: absolite; top: 10px; right: 12px;">
          <span aria-hidden="true" class="far fa-times"></span>
        </button>
        
        <div class="text-center">
          <h2 class="text-uppercase">Fully customised label quote</h2>
          <p class="lead mt-3">To have one of our designers help create a truly unique label for your brand, please fill out the below with your details and we'll be in touch.</p>
          <p>Don't worry about getting everything perfect, we'll work with you every step of the way.</p>
        </div>
        
        <hr>
        
        {% form 'contact' %}
          <div class="px-lg-mini">
          {% if form.posted_successfully? %}
            <script>
                window.location = "/pages/thankyou";
            </script>
          {% endif %}

          {% if form.errors %}
          <div class="form-errors text-danger text-center">
            <h4>Contact form errors:</h4>
            {{ form.errors | default_errors }}
          </div>
          {% endif %}
            
          <div class="form-group">
            <label for="ContactFormEmail">Quick Subject</label>
             <input type="text"
            class="form-control" 
            name="contact[subject]"
            id="ContactFormSubject"
            value="{% if form[subject] %}{{ form[subject] }}{% else %}Custom Logo Quote - {% endif %}">
          </div>
            
          <div class="row">
            
            {% if form.posted_successfully? != true %}
            <div class="form-group col-12 col-lg-6">
              <label for="ContactFormName">Your name</label>
              <input type="text"
              class="form-control" 
              name="contact[name]"
              id="ContactFormName"
              placeholder="{{ 'contact.form.name' | t }}"
              value="{% if form[name] %}{{ form[name] }}{% elsif customer %}{{ customer.name }}{% endif %}">
            </div>

            <div class="form-group col-12 col-lg-6">
              <label for="ContactFormEmail">Your email</label>
              <input type="email"
                class="form-control" 
                name="contact[email]"
                id="ContactFormEmail"
                placeholder="{{ 'contact.form.email' | t }}"
                value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
                spellcheck="false"
                autocomplete="off"
                autocapitalize="off">
            </div>
            
            <div class="form-group col-12 col-lg-6">
              <label for="ContactFormEmail">ABN (if available)</label>
              <input type="text"
              class="form-control" 
              name="contact[ABN]"
              id="ContactFormABN"
              value="{% if form[ABN] %}{{ form[ABN] }}{% endif %}">
            </div>

            <div class="form-group col-12 col-lg-6">
              <label for="ContactFormPhone">Your phone</label>
              <input type="tel"
                class="form-control" 
                name="contact[phone]"
                id="ContactFormPhone"
                placeholder="{{ 'contact.form.phone' | t }}"
                value="{% if form[phone] %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}"
                pattern="[0-9\-]*">
            </div>
            
          </div>
          
          <div class="form-group">
            <label for="ContactFormMessage">Your custom logo (if available)</label>
            <div class="logo_field w-100 pt-2"></div>
            <div class="upload-lift"></div>
          </div>

          <div class="form-group">
            <label for="ContactFormMessage">Some details of what you would like customised</label>
            <textarea rows="5" 
              class="form-control" 
              name="contact[body]"
              id="EnquiryFormMessage"
              placeholder="{{ 'contact.form.message' | t }}">
              {%- if form.body -%}
                {{- form.body -}}
              {%- endif -%}
            </textarea>
          </div>

          <div class="row">
            <div class="col-12 col-md-12 text-center">
                <input type="submit" class="btn btn-primary btn-lrg" style="padding-left: 30px; padding-right: 30px;" value="{{ 'contact.form.send' | t }}">
            </div>
          </div>

          {% endif %} <!-- If not submitted -->
            
          </div>
        {% endform %}
        
      </div>
      
      <div class="modal-footer justify-content-center">
        <button type="button" class="btn text-dark" data-dismiss="modal">
          Close <i class="fal fa-times ml-2"></i>
        </button>
      </div>
        
    </div>
  </div>
</div>